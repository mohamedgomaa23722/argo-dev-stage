group: example
stage: test
namespace: argocd
templateApp: user-template
container:
  name: user-service
domain: user

deployment:
  name: js-user-service-deployment

config:
  name: user-config-map-env
  volume:
    name: user-config-map

image:
  name: microservice-user:4

port: 8080
name: js-user-service

Payment:
  uri: test

order:
  uri: test

replicas: 1

service:
  name: user-svc
